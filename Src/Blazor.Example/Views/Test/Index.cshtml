@model TestViewModel

<h2>Test</h2>

@if (string.IsNullOrEmpty(Model.SavedState))
{
    <p>
        This is a test with nothing special.
    </p>
}
else
{
    <p>
        @Model.SavedState
    </p>
}